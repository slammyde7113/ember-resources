{{#link-to 'list' list}}
  <h3 class="list-header">{{list.title}}</h3>
{{/link-to}}
<button class="btn btn-primary btn-xs" {{action 'edit'}}>Edit</button>
{{#if list.isEmpty}}
<button class="btn btn-danger btn-xs" {{action 'delete'}}>Delete</button>
{{else}}
<p>List must be empty to delete</p>
{{/if}}
